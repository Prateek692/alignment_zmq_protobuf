# Send data back
    # processed_data = [[x + 1 for x in sublist] for sublist in received_data]
    # print("Sending processed data back:", processed_data)
    # send_vector_of_vectors(processed_data, sender)